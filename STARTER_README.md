# WISH BOYKE — Starter
Setelah membuat CRA, install:
```
npm i react-router-dom framer-motion lucide-react
npm i -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
Salin file dari paket starter ini (src, public, tailwind.config.js, postcss.config.js, netlify.toml, netlify/functions/*).
